# This is a demo JSON-like file that supports comments
{
    "endpoint": [{
            "uri": "/hello.elf",                        # The URI that, when visited, serves the content
            "dependencies": [{                          # A list of dependencies
                "sharedObject": "HelloWorldView.so",    # A shared object to be generatede
                "source": [                             # The source(s) that compose the shared object
                    "HelloWorldView.cpp"
                ]
            }]
        },
        {
            "uri": "/time.elf",
            "dependencies": [{
                "sharedObject": "TimeView.so",
                "source": [
                    "TimeView.cpp"
                ]
            }]
        },
        {
            "uri": "/peetah.elf",
            "dependencies": [{
                "sharedObject": "JoeyView.so",
                "source": [
                    "test_dir"
                ]
            }]
        },
        {
            "uri": "/ultimate.elf",
            "dependencies": [{
                "sharedObject": "TheUltimateView.so",
                "source": [
                    "TheUltimateView.cpp"
                ]
            }]
        }
    ]
}
